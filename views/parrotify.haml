!!!
= haml :generic_header
%body
  = haml :nav
  .container
    .starter
      %h1 Total (or parrot) Emojis as a Service! (TEaaS)

      %p Want to generate your own parrot-like emoji? Here's the place!
      %img(src="parrot.gif")

      %h2 LET THE FORM BEGIN!
      %form(method="POST" action="/parrotify" enctype="multipart/form-data")
        %p Note: File should not be animated (static GIFs are supported)
        = haml :upload_button
        = haml :_form_footer
