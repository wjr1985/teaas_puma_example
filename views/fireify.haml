!!!
= haml :generic_header
%body
  = haml :nav
  .container
    .starter
      %h1 Total(ly engulfed in flames) Emojis as a Service! (TEaaS)

      %p Catch your emojis on fire.
      %img(src="fire.gif")

      %p Only works best on square or square-ish images.

      %h2 LET THE FORM BEGIN!
      %form(method="POST" action="/fireify" enctype="multipart/form-data")
        %p Note: File should not be animated (static GIFs are supported)
        = haml :upload_button
        = haml :_form_footer
