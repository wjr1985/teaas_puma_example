!!!
= haml :generic_header
%body
  = haml :nav
  .container
    .starter
      %h1 Total Emojis as a Service! (TEaaS)

      %p Add some blood and fire to your emojis, GoT style.
      %img(src="blood.gif")
      %img(src="fire.gif")

      %p Warning: makes really large GIFs.

      %h2 LET THE FORM BEGIN!
      %form(method="POST" action="/gotify" enctype="multipart/form-data")
        %p Note: File should not be animated (static GIFs are supported)
        = haml :upload_button
        = haml :_form_footer
