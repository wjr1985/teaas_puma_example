<%= erb :generic_header %>
<body>
  <%= erb :nav %>
  <div class='container'>
    <div class='starter'>
      <h1>... (TEaaS)</h1>
      <p>Overlay some waiting on your emoji. Like this:</p>
      <img src='wait.gif'>
      <h2>LET THE FORM BEGIN!</h2>
      <form action='/waitify' enctype='multipart/form-data' method='POST'>
        <p>Note: File should not be animated (static GIFs are supported)</p>
        <%= erb :upload_button %>
        <%= erb :form_footer %>
      </form>
    </div>
  </div>
</body>

